/**
 * Log levels.
 * @notice useful to discriminate the log level for message printing.
 * @enum {string}
 */
export enum LogLevel {
    INFO = "INFO",
    DEBUG = "DEBUG",
    WARN = "WARN",
    ERROR = "ERROR",
    LOG = "LOG"
}
